
#
# {{ app.Description }}>
#
<VirtualHost *:{{ app.VirtualHostListen }}>
    ServerName {{ app.ServerName }}
    ServerAlias {{ app.ServerAlias }}
    ServerAdmin {{ app.ServerAdmin }}

    DocumentRoot "{{ app.ProjectRoot }}"
</VirtualHost>